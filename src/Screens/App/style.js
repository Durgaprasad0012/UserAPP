import { StyleSheet } from "react-native";

export const style = (width, height, isProtrait) => StyleSheet.create({
    title: {
        height: 25,
        width: 25,
        resizeMode: 'contain'
    },
})